/* LEARNED ORDER BY ,,,, */


SELECT      TOP 1
            CITY, MIN(LEN(CITY))
FROM        STATION
GROUP BY    CITY
ORDER BY    LEN(CITY), CITY
;

SELECT      TOP 1
            CITY, MAX(LEN(CITY))
FROM        STATION
GROUP BY    CITY
ORDER BY    LEN(CITY) DESC, CITY
;
