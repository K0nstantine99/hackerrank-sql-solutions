-- LEARNED CAST( AS DECIMAL(10,4)
--LEARNED bigger than smaller than can be used with AND

SELECT
            CAST(SUM(LAT_N) AS DECIMAL(10,4))
FROM        STATION
WHERE      LAT_N > 38.7880 AND 137.2345 > LAT_N
;
