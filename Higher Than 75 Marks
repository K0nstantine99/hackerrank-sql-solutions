-- ordering by right 3 characters and then ordering by ID. by default any order is ASCENDING, you can change to DESC.

SELECT
            Name
FROM        STUDENTS
WHERE       Marks > 75
ORDER BY    RIGHT(Name,3) ASC, ID ASC
;
